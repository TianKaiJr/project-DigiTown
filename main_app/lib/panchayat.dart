import 'package:flutter/material.dart';

class PanchayatPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Panchayat Page")),
      body: Center(
        child: Text("Welcome to the Panchayat Page"),
      ),
    );
  }
}
