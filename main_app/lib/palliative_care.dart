import 'package:flutter/material.dart';

class PalliativeCarePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Palliative Care Page")),
      body: Center(
        child: Text("Welcome to the Palliative Care Page"),
      ),
    );
  }
}
